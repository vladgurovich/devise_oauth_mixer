%h2
  Complete signup
= simple_form_for(resource, :as => resource_name, :url => complete_signup_path, :html => { :method => :put }) do |f|
  = f.error_notification
  .form-inputs
    = f.input :email, :required => true, :autofocus => true
  .form-actions
    = f.button :submit, "Complete"

